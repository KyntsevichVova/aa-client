import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

function AppHeader() {
    return (
        <Header style={{
            padding: 0,
            background: "#fff",
        }}>

        </Header>
    );
}

export default AppHeader;